;;; early-init --- Cade's Emacs Config -*- lexical-binding: t; -*-
;;; Commentary:
;; try to disable `package-enable-at-startup'.
;;; Code:

(setq package-enable-at-startup nil)

;;; early-init.el ends here
